export function getRobotsHandler(): void {
    // check which robots are not currently in use
    // optionally, filter by which robots are online
    // emit the response
}

export function selectRobotHandler(name: string): void {
    // make sure the robot is still available
    // reserve it
    // assign it to the player
    // broadcast player update
}